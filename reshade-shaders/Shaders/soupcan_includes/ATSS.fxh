// TAA, if you really prefer that name. 
// Not based on anything in specific, yet.

texture prev0 { Width = BUFFER_WIDTH * RENDER_SCALE; Height = BUFFER_HEIGHT * RENDER_SCALE; Format = RGBA16F; };

sampler sp0 { Texture = prev0; };
